# Prompt: Create `jref` Skill

- Create a skill folder that follows the standard "Skill" protocol for AI agents, which consists of a `SKILL.md` at base of `skill/` folder, with additional `assets/`, `references/`, `scripts/`, `prompts/`, etc..

- Within the `skill/` folder, make a *minimum* of a SKILL.md file, which outlines the usage of `jref`, both as a CLI tool, and also it's implementation as an MCP server via it's `jref serve` command.
  - This information/set of directives should be highly detailed, robust, and verbose.

- Create any other files/artifacts that may be considered necessary and/or beneficial to the skill and the agent's usage of `jref` overall.
  - This includes, but is not limited to, additional workflows/examples, preconfigured prompts to assist user and agent with invoking usage, additional scripts to include additional agent functionality of `jref` that is not explicitly featured within the tool itself, etc..
  - *Try* to include *at least* two additional files/artifacts to assist with integration and/or agentic use of the skill.
